// The auth system doesn't yet link a signed-in vendor account to one of the
// demo shops, so every vendor page operates on this representative shop
// until that link exists.
export const DEMO_SHOP_SLUG = "accra-tech-hub";
